apiVersion: v1
kind: Service
metadata:
  name: my-web-service
spec:
  type: NodePort
  selector:
    app: backend   # Deployment의 라벨과 일치
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080